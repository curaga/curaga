<%= form_with(model: @document, url: docs_path(@document)) do |f| %>
  <%= f.text_field :title, placeholder: 'Title' %>

  <div id="untitled-editor">
    <%= f.hidden_field :content %>
  </div>

  <%= f.button %>
<% end %>
